<%= form_for @world do |f| %>
  <%= f.text_field :countries, 'data-tokeninput' => { collection: Country.all, options: { theme: 'facebook', tokenValue: 'iso_code' } }.to_json %>
  <%= f.submit %>
<% end %>
